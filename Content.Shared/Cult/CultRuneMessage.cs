using Robust.Shared.Serialization;
using Robust.Shared.GameObjects;

namespace Content.Shared.Cult;

/*
[Serializable, NetSerializable]
public sealed class CultRuneInvokeEvent : EntityEventArgs
{
    public EntityUid Rune;
    public EntityUid User;
    public HashSet<EntityUid> Cultists = new HashSet<EntityUid>();

    public CultRuneInvokeEvent(EntityUid rune, EntityUid user, HashSet<EntityUid> cultists)
    {
        Rune = rune;
        User = user;
        Cultists = cultists;
    }
}

public sealed class CultRuneInvokeSuccessEvent : EntityEventArgs
{
    public EntityUid Rune;
    public EntityUid User;
    public HashSet<EntityUid> Cultists = new HashSet<EntityUid>();

    public CultRuneInvokeSuccessEvent(EntityUid rune, EntityUid user, HashSet<EntityUid> cultists)
    {
        Rune = rune;
        User = user;
        Cultists = cultists;
    }
}

*/
